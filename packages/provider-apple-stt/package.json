{
  "name": "@alfred/provider-apple-stt",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bash scripts/build-native.sh && tsc -p tsconfig.json",
    "build:native": "bash scripts/build-native.sh",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "postinstall": "bash scripts/build-native.sh || true"
  },
  "dependencies": {
    "@alfred/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
